<Window x:Class="ChromaScreen.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ChromaScreen - Window Selector" Height="550" Width="600"
        ResizeMode="CanMinimize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select a window to capture:" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListBox Grid.Row="1" x:Name="WindowListBox" DisplayMemberPath="Title" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Chroma Key Threshold:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Slider x:Name="ThresholdSlider" Width="200" Minimum="0" Maximum="100" Value="30"
                    TickFrequency="10" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ElementName=ThresholdSlider, Path=Value, StringFormat={}{0:F0}}"
                       VerticalAlignment="Center" Margin="10,0,0,0" Width="30"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Update Rate (ms):" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Slider x:Name="UpdateRateSlider" Width="200" Minimum="16" Maximum="500" Value="33"
                    TickFrequency="10" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ElementName=UpdateRateSlider, Path=Value, StringFormat={}{0:F0}}"
                       VerticalAlignment="Center" Margin="10,0,0,0" Width="30"/>
        </StackPanel>

        <Grid Grid.Row="4" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Crop Margins:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,5"/>

            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal">
                <TextBlock Text="Top:" VerticalAlignment="Center" Margin="0,0,5,0" Width="35"/>
                <TextBox x:Name="CropTopTextBox" Width="50" Text="36" VerticalAlignment="Center" Margin="0,0,10,0"/>

                <TextBlock Text="Left:" VerticalAlignment="Center" Margin="0,0,5,0" Width="35"/>
                <TextBox x:Name="CropLeftTextBox" Width="50" Text="1" VerticalAlignment="Center" Margin="0,0,10,0"/>

                <TextBlock Text="Right:" VerticalAlignment="Center" Margin="0,0,5,0" Width="35"/>
                <TextBox x:Name="CropRightTextBox" Width="50" Text="1" VerticalAlignment="Center" Margin="0,0,10,0"/>

                <TextBlock Text="Bottom:" VerticalAlignment="Center" Margin="0,0,5,0" Width="50"/>
                <TextBox x:Name="CropBottomTextBox" Width="50" Text="0" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="RefreshButton" Content="Refresh Windows" Width="120" Height="30"
                    Margin="0,0,10,0" Click="RefreshButton_Click"/>
            <Button x:Name="StartButton" Content="Start Overlay" Width="120" Height="30"
                    Click="StartButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
